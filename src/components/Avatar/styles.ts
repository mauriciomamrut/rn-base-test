import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderRadius: 28,
    height: 56,
    overflow: 'hidden',
    width: 56,
  },
  image: {
    height: '100%',
    resizeMode: 'cover',
    width: '100%',
  },
});

export default styles;
