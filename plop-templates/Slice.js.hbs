import { createSlice } from '@reduxjs/toolkit';

interface {{pascalCase name}}State {
  // Add state types
}

const {{camelCase name}}InitialState: {{pascalCase name}}State = {
  // Add initial state
};

const {{camelCase name}} = createSlice({
  name: '{{camelCase name}}',
  initialState: {{camelCase name}}InitialState,
  reducers: {
    // Add reducers
  },
});

export const {} = {{camelCase name}}.actions;

export default {{camelCase name}}.reducer;
